#!/usr/bin/env bash
#MISE description="Run the Docker-in-Docker integration suite (real Podman/systemd/WireGuard against a privileged container standing in for a remote host)"
set -e

compose_file="test/docker/compose.yml"

cleanup() {
    docker compose -f "$compose_file" down -t 0 -v
}
trap cleanup EXIT

docker compose -f "$compose_file" up -d --build --wait

# Each docker_*_test.rs file under crates/jiji-cli/tests/ is its own test binary; list new ones
# here as the suite grows (currently just Phase 1a's server-lifecycle test).
if command -v cargo-nextest >/dev/null 2>&1; then
    JIJI_DOCKER_TESTS=1 cargo nextest run -p jiji-cli --test docker_server_lifecycle_test --no-capture
else
    JIJI_DOCKER_TESTS=1 cargo test -p jiji-cli --test docker_server_lifecycle_test -- --nocapture
fi
