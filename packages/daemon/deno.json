{
  "name": "@jiji/daemon",
  "version": "0.4.0",
  "exports": "./src/main.ts",
  "tasks": {
    "check": "deno fmt --check && deno lint && deno test --allow-all --parallel tests/"
  },
  "compilerOptions": {
    "strict": true,
    "noImplicitAny": true
  }
}
